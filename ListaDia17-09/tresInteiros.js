//Crie um vetor de três números inteiros. Multiplique cada elemento por 2 e exiba o vetor resultante.

const prompt = require('prompt-sync')();

const inteiros = [10,20,30]

console.log(inteiros)

inteiros[0] = inteiros[0] * 2
inteiros[1] = inteiros[1] * 2
inteiros[2] = inteiros[2] * 2

console.log(inteiros)